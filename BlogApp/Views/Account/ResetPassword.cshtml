@model BlogApp.ViewModels.ResetPasswordViewModel

@{
    ViewBag.Title = "Delete Blog Post";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Delete Blog Post</h2>

<h3>Are you sure you want to reset the password of this user?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>

        
    </dl>

    @using (Html.BeginForm()){
        @Html.AntiForgeryToken()
        <div class="form-group">
            @Html.HiddenFor(model => model.Email)
            <input type="submit" value="Reset Password" class="btn btn-danger" /> |
            @Html.ActionLink("Back to List", "UserList")
        </div>
    }
</div>